Based on your requirements, I have analyzed the current system structure and prepared the following refactoring plan:

### 1. Database Schema Changes
We will modify the core tables to support the new fields and hierarchy.

*   **New Table: `exam_types`**
    *   Stores the customizable exam types (e.g., "After-school Knowledge", "Matching", etc.).
    *   Default data will be inserted.
*   **Update `public.profiles` (User)**
    *   Add `real_name` (Text).
    *   Add `vendor` (Text, initial).
    *   Add `education_stages` (Array, for multi-select).
    *   Add `subjects` (Array, for multi-select).
*   **Update `public.questions`**
    *   Add `exam_type_id` (Foreign Key) to link questions to the new top-level types.
*   **Update `public.quiz_configs` (Exam Paper)**
    *   Add `exam_type_id` (Foreign Key).
    *   Add `subject` (Text, moves from Part to Global).
    *   Add `grade_level` (Text, moves from Part to Global).

### 2. Logic & Access Control
*   **Exam Visibility**: Implement filtering logic so users only see exams that match their configured **Subjects** and **Education Stages**.
*   **Question Selection**: Update the question fetching algorithm to filter by:
    *   `Exam Type` (from Exam Config)
    *   `Subject` (from Exam Config)
    *   `Grade Level` (from Exam Config)
    *   `Part Attributes` (Difficulty, Type, Classification)

### 3. Frontend Implementation
*   **User Management**:
    *   Update Registration/Edit modal to include Real Name, Vendor, and multi-selects for Subjects and Grades.
*   **Exam Configuration (`QuizConfigForm`)**:
    *   **Global Settings**: Move Subject and Grade selection to the top. Add "Exam Type" selector.
    *   **Part Settings**: Remove Subject/Grade selectors. Keep Difficulty, Question Type, and Classification.
*   **Question Management**:
    *   Add "Exam Type" field to the Question Editor.
    *   **Batch Tool**: Create a "Batch Update" feature to assign Exam Types to existing questions (filtering by subject/grade to apply types quickly).
*   **User Dashboard**:
    *   Add filters for Exam Type, Subject, and Stage.
    *   Update Answer Record view with new filters (Grading Status, Vendor/Name search).

### 4. Migration Strategy
1.  **Execute SQL Migration**: Create tables and columns.
2.  **Code Update**: Deploy frontend and backend logic changes.
3.  **Data Fix**:
    *   Existing users will have empty new fields (admin needs to update them).
    *   Existing questions will have empty "Exam Type". You will use the new "Batch Update" tool to categorize them.
    *   Existing exams might need manual adjustment to set the global Subject/Grade.

Does this plan cover all your requirements? If confirmed, I will proceed with the database changes first.